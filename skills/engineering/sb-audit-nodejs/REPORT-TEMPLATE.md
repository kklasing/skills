# Tech Status Report — `<project name>`

> *This report was generated by Claude using the `sb-audit-nodejs` skill.*

**Date:** `<YYYY-MM-DD>`
**Repo:** `<git remote URL or path>`
**Commit:** `<short SHA>`

## Project shape

- **Framework:** `<NestJS / Next.js / Express / Fastify / generic Node / monorepo>`
- **Language:** `TypeScript (strict: yes/no) | JavaScript`
- **Node:** `<version pin or "unpinned">`
- **Package manager:** `<npm / pnpm / yarn>`
- **Test runner:** `<vitest / jest / mocha / none>`
- **Source layout:** `<src/ | apps/ | packages/>`

## High-risk summary

> Critical findings and high-severity security findings only. Anything in this section needs to be addressed soon.

1. **`<Title>`** — `<one-sentence impact>`. *Severity: critical · Effort: M · Confidence: certain*
2. ...

If empty: *No high-risk findings.*

## Maintainability

### What's good
- ...

### What's bad
- **`<Finding title>`** — `<description>`. *Severity / Effort / Confidence.* Evidence: `path/to/file.ts:42`, analyzer output, etc.

### What can be improved
- ...

## Security

### What's good
- ...

### What's bad
- **`<Finding title>`** — `<description>`. *Severity / Effort / Confidence.* Evidence: `npm audit` output, `path/to/file.ts:line`.

### What can be improved
- ...

## Scalability

### What's good
- ...

### What's bad
- **`<Finding title>`** — `<description>`. *Severity / Effort / Confidence.* Evidence: `path/to/file.ts:line`.

### What can be improved
- ...

## Improvement plan

> Structured for `/to-issues`. Each numbered item is a self-contained slice — pick one, ship it, move on. Keep titles imperative and concrete.

### P0 — high risk (do first)

#### 1. `<Imperative title, e.g. "Patch critical CVE in @nestjs/common chain">`
- **Perspective:** `security`
- **Severity:** `critical`
- **Effort:** `M`
- **Why:** `<one paragraph: what's broken, what risk it carries, what triggered the finding>`
- **Acceptance:**
  - [ ] `<criterion>`
  - [ ] `<criterion>`
- **Pointers:** `<file:line>`, `<file:line>`, `<analyzer output ref>`

#### 2. ...

### P1 — meaningful (do soon)

#### 1. `<Title>`
- **Perspective:** `maintainability | security | scalability`
- **Severity:** `high | medium`
- **Effort:** `S | M | L`
- **Why:** ...
- **Acceptance:**
  - [ ] ...
- **Pointers:** ...

### P2 — opportunistic (when touching the area)

#### 1. `<Title>`
- **Perspective:** ...
- **Severity:** `medium | low`
- **Effort:** `S | M`
- **Why:** `<one line is fine here>`
- **Pointers:** ...

## Methodology & gaps

**Tools run:**
- `npm audit` — `<ok / skipped: reason / failed: reason>`
- `depcheck` — ...
- `madge --circular` — ...
- `tsc --noEmit` — ...
- `eslint` — ...
- `npm outdated` — ...

**Not assessed (and why):**
- e.g. *"Tests not executed — DB credentials required."*
- e.g. *"Production deploy config not in repo — could not assess container limits or graceful shutdown."*
- e.g. *"Monorepo scope limited to `apps/api` — other packages out of scope for this run."*
